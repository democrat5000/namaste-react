import SearchBox from "./SearchBox";
import SearchButton from "./SearchButton";

const Search = ()=> {
  return(
    <div>
      <SearchBox/>
      <SearchButton/>
    </div>
  );
}

export default Search;